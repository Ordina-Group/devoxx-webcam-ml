{
  "name": "devoxx-webcam-ml",
  "version": "0.0.1",
  "description": "Devoxx demo application that detects objects in the live webcam view",
  "main": "src/index.js",
  "scripts": {
    "postinstall": "tsc",
    "compile": "tsc",
    "electron": "tsc && electron .",
    "electron_mac": "tsc && open node_modules/electron/dist/Electron.app --args $PWD"
  },
  "engines": {
    "node": ">=12.13.0"
  },
  "dependencies": {
    "@tensorflow/tfjs": "^1.2.3",
    "typescript": "^3.6.4"
  },
  "devDependencies": {
    "electron": "7.0.0",
    "electron-packager": "^14.0.6"
  }
}
